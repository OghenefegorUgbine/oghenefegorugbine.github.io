# Projects folder

Reserved for future standalone project pages (e.g. `projects/machining-simulation.html`)
if you later want a dedicated deep-dive page per project instead of the
summary cards on the homepage's Projects section. Currently empty, all
project content lives directly in `index.html`.
